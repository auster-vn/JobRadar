import type {Metadata} from "next";
import {ArrowDownRight, ArrowUpRight} from "lucide-react";
import {SalaryTrendChart} from "@/components/SalaryTrendChart";
import {SkillChart} from "@/components/SkillChart";
import {getSkillDemand} from "@/lib/api";
export const metadata: Metadata = {title: "Thị trường"};
export default async function MarketPage() { const skills = await getSkillDemand(12); const rows = skills.length ? skills : [{skill:"Python",job_count:184,demand_rank:1,mom_growth_pct:12.4},{skill:"Java",job_count:163,demand_rank:2,mom_growth_pct:6.8},{skill:"React",job_count:151,demand_rank:3,mom_growth_pct:9.2},{skill:"AWS",job_count:124,demand_rank:4,mom_growth_pct:14.1},{skill:"SQL",job_count:117,demand_rank:5,mom_growth_pct:-2.3}]; return <div className="page-stack"><section className="page-heading compact"><div><p className="eyebrow">Market intelligence</p><h1>Nhịp đập thị trường</h1><p>Nhu cầu kỹ năng và chuyển động mức lương theo thời gian.</p></div></section><section className="dashboard-grid"><article className="panel"><div className="panel-heading"><div><h2>Nhu cầu kỹ năng</h2><p>90 ngày gần nhất</p></div></div><SkillChart data={rows} /></article><article className="panel"><div className="panel-heading"><div><h2>Lương trung vị</h2><p>Sáu tháng gần nhất</p></div></div><SalaryTrendChart /></article></section><section className="panel market-table"><div className="panel-heading"><div><h2>Xếp hạng kỹ năng</h2><p>Khối lượng tin tuyển dụng và tăng trưởng tháng</p></div></div><div className="table-head"><span>Hạng</span><span>Kỹ năng</span><span>Tin tuyển dụng</span><span>Tăng trưởng</span></div>{rows.map((row) => <div className="table-row" key={row.skill}><span>#{row.demand_rank}</span><strong>{row.skill}</strong><span>{row.job_count}</span><span className={(row.mom_growth_pct ?? 0) >= 0 ? "positive" : "negative"}>{(row.mom_growth_pct ?? 0) >= 0 ? <ArrowUpRight size={15} /> : <ArrowDownRight size={15} />}{Math.abs(row.mom_growth_pct ?? 0)}%</span></div>)}</section></div>; }
